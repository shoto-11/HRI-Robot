# 工場内 AGV 実験 — 実装書類

> **目的**: 本プロジェクト（`HRI-Robot`）に**実際に実装されている内容**をまとめたドキュメント。  
> **更新方針**: 機能追加・仕様変更があったら、このファイルを随時更新する。設計意図の詳細は `CLAUDE.md`（実装仕様書）も参照。

**最終更新**: 2026-08-20  
**対象シーン**: `Assets/Scenes/FactoryExperiment.unity`  
**ランタイム**: Unity（URP）+ OpenXR（Meta Quest 3 想定）、PC キーボードでも操作可能

---

## 1. 実験概要

工場内を走行する AGV（10〜20 台）が荷物のピックアップ→ドロップを反復する環境で、参加者が **Station A → Station B** へ移動する。AGV 走行経路の可視化方式が、移動時間・ニアミス回数・移動距離に与える影響を調べる VR 実験。

| 条件 | 内部名 | 経路表示 |
|------|--------|----------|
| ベースライン | `Baseline` | 全 AGV 同じ見た目（単色・不透明度 1.0） |
| no-ar | `NoAR` | 経路ライン非表示 |
| 提案手法 | `Proposed` | 危険度に応じて色・不透明度を変化 |

同一ケース番号なら **シード固定** により AGV の台数・速度・動きは 3 条件で一致する。

---

## 2. シーン構成

### 2.1 主要 GameObject

| オブジェクト | コンポーネント | 役割 |
|-------------|---------------|------|
| `XR Origin` | `CharacterController`, `PlayerLocomotion`, `DynamicCrossingLineTracker` | 参加者（PC/VR） |
| `ExperimentSystems` | `ExperimentManager`, `ExperimentStartMenu`, `MeasurementHub`, `AGVSpawner`, `AGVPathVisualizer`, `PathDeviationTracker` 等 | 実験制御・計測 |
| `FactoryData` | `FactoryData` | ワークベンチ座標など |
| `BoxPickupPool` | `BoxPickupPool` | 荷物箱プール（実行時生成） |
| `AGVRoot` | （子に AGV） | AGV 親（`AGVSpawner` 配下、実行時生成） |

### 2.2 座標（`FactoryLayout.cs`）

| 名称 | 座標 |
|------|------|
| Station A（開始） | `(22, 0.2, 9)` |
| Station B（ゴール） | `(9, 0.2, 58)` |
| AGV 床面 Y | `0.15`（`AgvFloorY`） |
| 標準開始向き | 最初の通路 WP `(21.475, 12)` 方向（`SpawnRotation`） |

### 2.3 エディタセットアップ

`FactoryHRISetup.cs` により倉庫アセット（`UnityWarehouseSceneHDRP`）またはグレーボックス工場を構築。

**主なメニュー（`HRI`）**

| メニュー | 内容 |
|---------|------|
| `Setup Factory Scene` | 倉庫シーン構築（URP 化・NavMesh・Station・プレイヤー・Systems） |
| `Setup Factory Scene (Graybox)` | アセットなし時の簡易工場 |
| `Restore Warehouse Building` / `Restore Original Warehouse Look` | 建物メッシュ復元 |
| `Fix URP Materials` / `Fix Pink Shelves` 等 | マテリアル・ライティング修復 |
| `Test Experiment Workbook` | 評価 xlsx の書き出しテスト |

---

## 3. 実験フロー

```
Play 開始
  └─ 画面オーバーレイ「実験コントロール」（条件フォルダ選択）
       └─ ケース 1〜10 選択
            └─ 3 秒イントロ UI → ケース実行（Station A → B）
                 └─ ゴール到着
                      ├─ 評価データ保存（xlsx 追記）
                      ├─ プレイヤーを Station A に復帰（位置・向きリセット）
                      ├─ AGV 全消去
                      └─ ケース選択画面へ自動復帰（次ケースは手動選択）
```

### 3.1 `ExperimentManager.cs`

- **ケースシード**: 10 個固定（Inspector の `seeds` 配列）
- **状態**: `Idle` → `Intro`（3 秒）→ `Running` →（ゴール）→ `Idle`
- **ゴール判定**: XR Origin の XZ 距離が Station B 半径 3 m 以内
- **テレポート** (`TeleportPlayer`): 位置 + `FactoryLayout.SpawnRotation` + カメラピッチリセット

### 3.2 `ExperimentStartMenu.cs`

- **表示**: `ScreenSpaceOverlay`（Game ビュー全面、PC マウスクリック対応）
- **2 段階 UI**:
  1. 条件フォルダ（ベースライン / no-ar / 提案手法）
  2. ケース 1〜10（seed・AGV 台数・歩行速度を表示）
- **キーボード**: フォルダ `1/2/3`、ケース `1〜9/0`、開始 `Enter/Space`、戻る `Esc`
- **メニュー表示中**: 移動入力無効、カーソル表示

---

## 4. AGV システム

### 4.1 スクリプト対応

| ファイル | 役割 |
|---------|------|
| `AGVAgent.cs` | 地上移動・ミッション実行、`IsStopped` / `plannedPath` / `currentSpeed` を公開 |
| `AGVFleetOrchestrator.cs` | 全 AGV を **0.1 秒固定クロック**で一括更新 |
| `AGVSpawner.cs` | シードから台数（10〜20）・速度（1.0〜2.0 m/s）を決定論的算出 |
| `AGVMissionPlan.cs` / `AGVMotionSimulator.cs` | フェーズ: `MovingToPickup` → `DwellAtPickup` → `MovingToDrop` → `DwellAtDrop` |
| `AGVRoutePlanner.cs` | NavMesh 経路（Y は `AgvFloorY` に固定） |
| `BoxPickupPool.cs` | ピックアップ箱の Claim/Return。AGV 台数に足りなければ `EnsureCount` で追加生成 |

### 4.2 ミッション

- 各 AGV: ピックアップゾーン → ドロップゾーンを反復
- 停止（dwell）時間: ピックアップ・ドロップ各 **2.0 秒**（仮値、`AGVMissionPlan`）
- 見た目サイズ: **約 1.0 × 1.0 × 0.2 m**（幅×奥行×高、`AGVSpawner.agvVisualScale`）
- ケース開始: `AGVSpawner.RestartWithSeed(seed)`
- ケース完了: `AGVSpawner.ClearSession()` で AGV のみ削除（Systems UI は残す）

### 4.3 AGV ID

- 表示名: `AGV_01` … `AGV_NN`
- 評価データの `RobotId`: `AGVAgent.Index + 1`（1 始まり）

---

## 5. 経路可視化（3 条件）

### 5.1 `AGVPathVisualizer.cs`

| モード | 動作 |
|--------|------|
| `Baseline` | `VehicleRiskCalculator` 有効、`SkipScoring=true`、スコア常に 1.0 |
| `NoAR` | 全 AGV の `PathRenderer` 非表示 |
| `Proposed` | 危険度連動フル稼働 |

メニュー表示中はキーボード `1/2/3` による条件切替を無効化。

### 5.2 `PathRenderer.cs`

- **表示距離**: プレイヤーから水平 **13.6 m 以内**の AGV のみ（(2.0 + 1.4) × 4 s）
- **走行中**: 残り経路全体を三角テーパー（根元 0.65 m → 先端 0.12 m）
- **停止中**: 停止線（全幅 1.0 m）
- **Baseline**: 固定色 `(0.25, 0.55, 1.0)`、alpha=1.0

### 5.3 `RiskToVisualMapper.cs`

- 色相: 赤（危険）→ 薄青緑（安全）
- 不透明度: 4 段階（0.35 / 0.55 / 0.80 / 1.0）

---

## 6. 危険度算出

### 6.1 `DynamicCrossingLineTracker.cs`

- HMD／カメラの **視線方向**（XZ 投影）へレイキャストし、棚・壁などに当たるまでの **太線基準ゾーン** を毎フレーム更新
- 半幅 **0.5 m**（全幅 1.0 m）。軸は `AxisStart`（参加者位置）→ `AxisEnd`（hit 点の床面投影）
- Station B への NavMesh 最短経路は **使用しない**

### 6.2 `VehicleRiskCalculator.cs`

| パラメータ | 値 |
|-----------|-----|
| TTC 上限 | 4 秒 |
| 表示距離 | 13.6 m（(v_AGV,max + v_ped) × T_max） |
| γ | 0.6 |
| スコア下限 | 0.08 |

- TTC: AGV 残り経路と視線方向太線ゾーンの交差までの時間
- 近接: 参加者との距離
- `currentScore = max(TTC スコア, 近接スコア, 下限)`

---

## 7. プレイヤー操作

### 7.1 `PlayerLocomotion.cs`

| 操作 | PC |
|------|-----|
| 移動 | WASD（HMD/カメラ forward 基準） |
| 左右回転 | ← → |
| 上下視点 | ↑ ↓ |
| 歩行速度 | 1.4 m/s（Inspector 可変） |

- メニュー表示中は移動・回転入力をスキップ
- `ResetView()`: カメラピッチ 0° にリセット（ケース開始時に呼ばれる）

### 7.2 `ComfortVignette.cs`

- VR 酔い対策の周辺減光（常時弱め + 回転速度連動）

---

## 8. 計測・評価データ

### 8.1 計測コンポーネント（`MeasurementHub.cs` 配下）

| コンポーネント | 計測内容 |
|---------------|---------|
| `TaskTimer` | ケース完了時間（秒） |
| `CollisionCounter` | ニアミス回数（AGV 半径 0.5 m + プレイヤー 0.3 m、AGV ごとクールダウン 1.5 s） |
| `PathDeviationTracker` | 参加者の移動距離（水平・m）、0.5 秒間隔で位置サンプル |

### 8.2 出力ファイル

- **1 Play セッション = 1 ファイル**
- パス: `{persistentDataPath}/HRI_AGV_Result_{yyyyMMdd_HHmmss}.xlsx`
  - Windows 例: `C:\Users\<user>\AppData\LocalLow\DefaultCompany\HRI-Robot\`
- **ケース完了ごとに上書き保存**（Summary + ケースシートを増やしていく）

### 8.3 Summary シート

| 列 | 内容 |
|----|------|
| SessionID | セッション ID |
| Timestamp | ケース完了日時 |
| Condition | Baseline / NoAR / Proposed |
| CaseIndex | 1〜10 |
| CompletionTime_s | 所要時間 |
| Collisions | ニアミス回数 |
| TraveledPath_m | 移動した経路の長さ（水平距離の合計、m） |
| CollisionEvents | 衝突詳細（下記形式） |

**CollisionEvents 形式**（衝突なしは `()`）:

```
((時刻秒,RobotId,x,y,z),(時刻秒,RobotId,x,y,z),...)
```

例: `((3.20,5,18.100,0.200,22.000),(8.05,12,9.500,0.180,40.250))`

### 8.4 ケース別シート

- シート名: `{Condition}_Case{01}`（重複時は `_2` 等を付与）
- 列: `Time_s`, `PosX`, `PosY`, `PosZ`（0.5 秒間隔の参加者位置）

### 8.5 実装: `ExperimentWorkbook.cs`

- Office Open XML（`.xlsx`）を Zip で直接生成（外部ライブラリ不要）

---

## 9. インフラ・補助

| ファイル | 役割 |
|---------|------|
| `UiEventSystemBootstrap.cs` | Input System 用 EventSystem 自動生成 |
| `URPMaterialFixer.cs` | HDRP→URP マテリアル修復 |
| `VisualizationLayers.cs` | SceneViewOnly レイヤー管理 |
| `VisualizationLayers` / `CargoZoneMarker` | ピックアップ・ドロップゾーン表示 |

---

## 10. ソースファイル一覧

```
Assets/Scripts/Runtime/
  ExperimentManager.cs       … 実験フロー・ゴール判定・テレポート
  ExperimentStartMenu.cs     … 二段階オーバーレイメニュー
  MeasurementHub.cs          … 計測統合・xlsx 保存
  ExperimentWorkbook.cs      … xlsx 書き出し
  AGVAgent.cs                … AGV エージェント
  AGVSpawner.cs              … AGV 生成
  AGVFleetOrchestrator.cs    … 一括更新
  AGVMissionPlan.cs          … ミッション定義
  AGVMotionSimulator.cs      … 移動シミュレーション
  AGVRoutePlanner.cs         … NavMesh 経路
  AGVPathVisualizer.cs       … 3 条件切替
  PathRenderer.cs            … 経路描画
  VehicleRiskCalculator.cs   … 危険度
  RiskToVisualMapper.cs      … 色・透明度マッピング
  DynamicCrossingLineTracker.cs … 動的基準線
  PathDeviationTracker.cs    … 移動距離・軌跡
  CollisionCounter.cs        … ニアミス
  TaskTimer.cs               … タイマー
  PlayerLocomotion.cs        … 移動
  FactoryLayout.cs           … 座標定数
  FactoryData.cs             … データ保持
  BoxPickupPool.cs           … 荷物箱
  CargoZoneMarker.cs         … ゾーンマーカー
  UiEventSystemBootstrap.cs
  URPMaterialFixer.cs
  VisualizationLayers.cs
  ComfortVignette.cs

Assets/Scripts/Editor/
  FactoryHRISetup.cs         … シーン構築
  ExperimentDataTest.cs      … xlsx テスト
  EditorNavMeshUtility.cs    … NavMesh ベイク
  ProjectSetup.cs            … URP/XR 初期設定
```

---

## 11. パラメータ早見表

| 項目 | 値 | 定義場所 |
|------|-----|---------|
| AGV 台数 | 10〜20（シード依存） | `AGVSpawner` |
| AGV 速度 | 1.0〜2.0 m/s | `AGVSpawner` |
| AGV 見た目サイズ | 約 1.0×1.0×0.2 m | `AGVSpawner.agvVisualScale` |
| 歩行速度 | 1.4 m/s | `PlayerLocomotion` |
| ニアミス AGV 半径 | 0.5 m（床面 1.0 m の半分） | `CollisionCounter` / `FactoryLayout.AgvFootprintM` |
| ニアミス プレイヤー半径 | 0.3 m | `CollisionCounter.bodyRadius` |
| ニアミス 合計しきい値 | 0.8 m | AGV 半径 + プレイヤー半径 |
| 軌跡記録間隔 | 0.5 s | `PathDeviationTracker` |
| 経路表示距離 | 13.6 m | `FactoryLayout.DisplayDistanceMax` |
| 停止線全幅 | 1.0 m（半幅 0.5 m） | `PathRenderer` |
| TTC 上限 | 4 s | `VehicleRiskCalculator` |
| dwell 時間 | 2.0 s | `AGVMissionPlan` |
| ゴール半径 | 3 m | `ExperimentManager` |
| イントロ表示 | 3 s | `ExperimentManager` |

---

## 12. 既知の制約・未決事項

- dwell 時間 2.0 s は仮値（予備実験で調整予定）
- 実験順序（ラテン方格）はメニュー UI では案内のみ。自動制御は未実装
- VR 実機（Quest 3）での最終確認は別途推奨
- `collision_log.txt` はデバッグ用テキストログ（xlsx とは別）

---

## 13. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-20 | 初版作成。二段階メニュー、xlsx 評価、CollisionEvents 列、スポーン位置・向きリセット、ScreenSpaceOverlay UI を反映 |
| 2026-08-20 | ニアミス判定を AGV 床面 1.0 m 四方に合わせ（半径 0.5 m + プレイヤー 0.3 m） |
| 2026-08-20 | AGV 見た目を約 1.0×1.0×0.2 m に変更（`agvVisualScale`） |
| 2026-08-20 | Summary 列 `PathDeviation_m` を `TraveledPath_m`（移動経路の長さ）に変更 |
| 2026-08-20 | 停止線の全幅を 1.0 m に変更（半幅 0.5 m） |
| 2026-08-20 | スポーンをポール横・通路方向へ 2 m オフセット、床スナップ強化 |
| 2026-08-20 | ケース再開始時のスポーン段差を修正（NavMesh スナップ、メニュー中重力停止） |
| 2026-08-20 | 基準線を視線レイ＋太線（半幅 0.5 m）方式に変更 |
| 2026-08-20 | 近接表示上限を (2.0+1.4)×4 = 13.6 m に変更（`FactoryLayout.DisplayDistanceMax`） |

---

## 14. 関連ドキュメント

- `CLAUDE.md` — 設計仕様・移行前計画を含む統合仕様書
- Unity メニュー `HRI > Test Experiment Workbook` — 評価ファイルの動作確認
