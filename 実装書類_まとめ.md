# 実装のまとめ（かんたん版）

このプロジェクトで **いま動いている実験プログラム** の説明です。  
ファイル名・座標の一覧は `実装書類.md` を見てください。

**使うシーン**: `FactoryExperiment`  
**動かし方**: Unity で Play。キーボード（WASD）でも、Quest 3 でも操作できます。

---

## 実験でやること

工場の VR の中で、人が **Station A → Station B** まで歩きます。  
まわりではロボット（AGV）が荷物を運んでいます（だいたい 10〜20 台）。

調べたいのは、ロボットの進路の見せ方を変えると、

- ゴールまでの時間
- ニアミス（近づきすぎ）の回数
- 歩いた距離

がどう変わるか、です。

---

## 3 つの見せ方

同じケース番号なら、ロボットの台数・速さ・動きは **いつも同じ** です。違うのは「道が見えるかどうか・色が変わるかどうか」だけです。

| メニューの名前 | 見えるもの |
|----------------|------------|
| ベースライン | 全部の道が同じ青い線 |
| no-ar | 道は出ない |
| 提案手法 | 危ないロボットほど赤く・濃く見える |

---

## 実験の進め方

1. Play を押すと、画面にメニューが出る
2. まず条件（上の 3 つのどれか）を選ぶ
3. つぎにケース 1〜10 を選ぶ
4. 3 秒の案内のあと、Station A から歩き始める
5. Station B の近く（半径 3 m）に着くと 1 ケース終了
6. 結果が Excel に保存され、メニューに戻る（次のケースは自分で選ぶ）

**キーボード**: 条件は `1` `2` `3`、ケースは `1`〜`9` と `0`（10）、開始は Enter / Space、戻るは Esc。

メニューが出ているあいだは歩けません。

---

## 歩き方（PC）

| キー | 動作 |
|------|------|
| WASD | 移動（カメラの向き基準） |
| ← → | 左右を向く |
| ↑ ↓ | 上下を向く |

歩く速さは 1.4 m/s です。

---

## ロボットの動き

- 荷物置き場へ行く → 2 秒止まる → 置き場へ運ぶ → 2 秒止まる、をくり返す
- 大きさはだいたい **1 m × 1 m、高さ 20 cm**
- 速さはケースごとに 1.0〜2.0 m/s（乱数ではなく、ケース番号で決まる）

---

## 提案手法の表示（実装側）

- 人が **今見ている方向** に、幅 1 m の帯を置く
- ロボットの進路がその帯に入りそうか、人と近いか、で色を決める
- 線はロボットの **前方・床付近** から出す（本体にかぶらないようにする）
- 人から 13.6 m より遠いロボットは出さない

---

## 記録されるデータ

1 回 Play するごとに、Excel が 1 つできます。

保存場所の例（Windows）:

`C:\Users\<ユーザー名>\AppData\LocalLow\DefaultCompany\HRI-Robot\`

ファイル名: `HRI_AGV_Result_日時.xlsx`

| 記録するもの | 意味 |
|--------------|------|
| 所要時間 | A から B までの秒数 |
| ニアミス回数 | 人とロボットがおよそ 0.8 m 以内に近づいた回数 |
| 移動距離 | 実際に歩いた長さ（m） |
| 軌跡 | 0.5 秒ごとの位置 |

ケースが終わるたびに、同じファイルへ追記されます。

---

## 自分でシーンを作り直すとき

Unity メニュー **HRI → Setup Factory Scene** を使います。  
倉庫の 3D アセットが無いときは **Setup Factory Scene (Graybox)** で簡易工場になります。
